# Карта базы знаний (навигация по проекту)

> Резервный план: если контекст потерян — читать этот файл, прежде чем продолжать работу.
> Обновляется при каждом добавлении/удалении файлов и папок верхнего уровня (см. `CLAUDE.md`).
> Последнее обновление: 2026-07-19.

**Проект целиком на `E:\Digital brain\`** (перенесён с `C:` 2026-07-12, см. историю ниже;
старая папка на `C:` удалена после перезагрузки Windows — процессы, державшие файлы,
были сняты рестартом).

```
E:\Digital brain\
├── CLAUDE.md                          — правила работы агента в проекте (читать первым)
├── AGENTS.md                          — те же правила для Codex CLI (дублирует CLAUDE.md)
├── карта_бз.md                        — этот файл
├── Журнал знаний.md                   — авто-лог изменений в raw-data-repository/ (не редактировать вручную)
├── Разработкаи Обращение к АИ/        — история исходных запросов пользователя (переехала из корня)
│   ├── Запросы и обращение к АИ.txt
│   └── Запросы к АИ ... Выжимка по урокам.txt
├── docs/superpowers/                  — специи и планы фич (spec → plan → subagent-driven-development)
│
├── project-config/                    — техническая архитектура и статус
│   ├── РЕШЕНИЯ-И-СТАТУС.md            — что сделано, какие решения приняты и почему
│   ├── Архитектура проекта. Настройка и Правила/
│   │   ├── core.md                    — стек, профили RAM (LIGHT/FULL), схема Knowledge Factory
│   │   ├── rules.md                   — роль агента, слои архитектуры, PRE-FLIGHT, отчётность
│   │   ├── ПЕРЕД-СТАРТОМ.md           — памятка запуска, частые ошибки, чек-лист
│   │   ├── Knowledge Factory.html     — вспомогательный файл (дублирует core/rules)
│   │   └── База_знаний_автодобавление_и_Косяк_2_Презентация.pdf
│   └── Транскрибированная лекция видео/
│       ├── База_знаний_автодобавление_и_Косяк_2_*.docx      — регламент автодобавления, локально/глобально
│       └── Создание_и_оптимизация_базы_знаний_в_Docker_*.docx — почему RAG+GraphRAG, 11 шагов фабрики
│
├── raw-data-repository/               — человекочитаемый vault (аналог Obsidian)
│   ├── README.md                      — вход в vault
│   ├── Навигация.md                   — рабочий цикл, направления
│   ├── 001 Входящие (Сырые данные)/   — быстрый сбор всего нового (включая Закладки браузера)
│   ├── 002 Мышление (Карта личности)/ — интересы, ценности, стиль мышления
│   ├── 003 Знания/                    — заметки, конспекты, модели
│   ├── 004 Инструменты/               — софт, промпты, AI-скиллы в реальном использовании
│   ├── 005 Ресурсы/                   — книги, курсы, референсы
│   ├── 006 Задачи (Цели)/             — README_Задачи.md + README_Цели.md
│   ├── 007 Проекты/                   — по направлениям: Веб/Видео/Аудио/3D/Game/Маркетинг
│   ├── 008 Интересы (Увлечения)/      — хобби вне рабочих направлений
│   └── 010 Архив (Неиспользуемое)/    — завершённое + Шаблоны (Идея/Проект/Задача/Заметка)
│
└── knowledge-factory/                 — Docker-бэкенд семантического поиска + kf.py + MCP-сервер
    ├── docker-compose.yml             — Qdrant + Postgres/pgvector + MinIO + Redis (LIGHT)
    ├── .env                           — DATA_ROOT, локальные пароли (не в git)
    ├── .env.example                   — шаблон .env
    ├── kf/                            — исходный код: scope, chunking, extract, store/*, api, cli, mcp_server,
    │                                    ocr (Tesseract), vision_caption (OpenRouter), video (ffmpeg),
    │                                    transcribe (faster-whisper), graph (Kuzu), embedding_models/
    │                                    embedding_state/embedding_sync (переключаемые модели эмбеддинга),
    │                                    web_extract (ссылки: trafilatura/youtube-transcript-api/yt-dlp)
    ├── tests/                         — 178 тестов (TDD, часть — против живого стека)
    ├── kf.py                          — CLI-вход: ingest, ingest-url, search, ask, stats, graph,
    │                                    embedding-model list/use/sync. ingest понимает .png/.jpg/.jpeg/
    │                                    .webp, .mp4/.mov/.mkv/.webm, .mp3/.wav/.ogg/.m4a, .xlsx
    ├── README.md                      — как поднять стек и пользоваться
    └── data/                          — вектора/кэш (437MB+, растёт). Не в git (.gitignore).
        (qdrant/, minio/, redis/, graph/, model-cache/ — Postgres НЕ здесь, он на именованном
         томе Docker kf_postgres_data — Windows/NTFS не даёт контейнеру нужных Unix-прав)
```

Рядом на `E:\` (вне папки `Digital brain`) лежит **«Игры пс4»** — посторонняя папка,
вне рамок проекта, не сканировать/не трогать.

## Куда класть новое (быстрый ориентир)

**Тема важнее формата файла.** Сначала проверь, относится ли материал к одной из уже
существующих тем ниже — если да, кладём туда независимо от того, PDF это, видео, ссылка,
аудио или текст. Если тема ни с чем не совпадает — используем общее правило по типу
материала.

### По теме (если материал явно про одно из направлений)

| Тема | Куда |
|---|---|
| Веб-дизайн | `007 Проекты/001 Веб Дизайн Web Design/` |
| Видео-дизайн | `007 Проекты/002 Видео Дизайн Video Design/` |
| Аудио-дизайн | `007 Проекты/003 Аудио дизайн Audio Designe & Production/` |
| 3D-моделирование | `007 Проекты/004 3D Моделирование 3D Modeling/` |
| Game-дизайн | `007 Проекты/005 Game Дизайн Game Design/` |
| Маркетинг | `007 Проекты/006 Маркетинг Marketing/` |
| Хобби вне рабочих направлений (без цели делать проект) | `008 Интересы (Увлечения)/` |

### По типу материала (если тема не совпадает ни с одним разделом выше)

| Тип материала | Куда |
|---|---|
| Сырое, не разобрано | `raw-data-repository/001 Входящие (Сырые данные)/` |
| Про себя, ценности, стиль мышления | `002 Мышление (Карта личности)/` |
| Конспект, модель, наблюдение | `003 Знания/` |
| Софт/промпт/скилл, которым реально пользуюсь | `004 Инструменты/` |
| Книга, курс, статья, референс (PDF, docx, ссылка, видео-курс) | `005 Ресурсы/` |
| Конкретное действие / цель | `006 Задачи (Цели)/` |
| Активный или будущий проект (не подходящий ни под одну тему выше) | `007 Проекты/` |
| Завершено / больше не актуально | `010 Архив (Неиспользуемое)/` |
| Настройки/архитектура самого проекта | `project-config/` |

Формат файла (PDF/docx/xlsx/видео/аудио/изображение и т.д.) определяет только то, **как**
материал обрабатывается технически при индексации (`kf/extract.py`: текст читается напрямую,
видео — транскрипция + разбор кадров, изображение — OCR/vision-caption, xlsx — построчное
чтение ячеек и т.д.) — не то, в какой раздел он попадает.

**Ссылки** (на статьи и на YouTube-видео) — не читаются автоматически при простом добавлении
файла в vault, для них есть отдельная команда: `kf.py ingest-url <ссылка> --dest <раздел>`
(сама скачивает, извлекает текст через `trafilatura`/субтитры/Whisper и индексирует). Если
извлечённый текст статьи получился подозрительно скудным — команда предупредит об этом в
выводе; в этом случае ассистент вручную достаёт текст через Firecrawl и заменяет заметку.

## Журнал знаний (Журнал знаний.md)

Автоматический лог каждого добавления/изменения/удаления файла в `raw-data-repository/`.
Пополняется сам при каждом запуске `kf.py ingest` (в т.ч. если файлы менялись вручную,
напрямую в Obsidian, без участия ассистента) — редактировать вручную не нужно и не имеет
смысла, правки перезапишутся при следующем `ingest`. При обнаружении удалённых файлов
запись попадает в журнал, но векторы/строки в Qdrant/Postgres/MinIO не удаляются
автоматически при обычном `ingest`.

Для реальной очистки — `kf.py sync-deletions` (сначала без флагов — покажет план и отличит
реальное удаление от переименования по сравнению хэша содержимого; `--yes` — выполняет).
Чистит и исходный файл, и его синтезированную заметку, во всех коллекциях Qdrant по всем
профилям эмбеддинга, в MinIO и в графе знаний.

## Граф знаний (сущности и связи)

`kf.py ingest` автоматически извлекает сущности (люди/инструменты/проекты/темы/концепты) и
прямые связи между ними из каждого файла, хранит в Kuzu (`knowledge-factory/data/graph/`).
Посмотреть связи сущности: `kf.py graph "название"` или MCP-инструмент `graph_search`.
Multi-hop запросы и визуализация графа — не реализованы, запланированы как надстройка над
той же графовой схемой в будущем.

## Переключаемые модели эмбеддинга

Четыре профиля модели эмбеддинга (`local`, `qwen3-8b`, `openai-small`, `openai-large`) — у
каждого своя коллекция в Qdrant, переключение на уже использовавшийся профиль мгновенное.
`kf.py embedding-model list/use/sync`: `use` переключает и бесплатно проверяет покрытие,
`sync` — единственная команда, которая реально тратит API-бюджет OpenRouter, досчитывая
только недостающие файлы.

## Расширение форматов (ссылки, аудио, Excel)

`kf.py ingest-url <ссылка> --dest <раздел>` — скачивает статью (`trafilatura`) или
YouTube-видео (сначала готовые субтитры, при их отсутствии — `yt-dlp` + Whisper), сохраняет
как файл в vault и сразу индексирует через обычный пайплайн. Проверено на реальных данных
2026-07-19 (статья с Wikipedia + видео с YouTube — оба пути отработали, ключи в базе
корректны). Отдельные аудиофайлы (`.mp3`/`.wav`/`.ogg`/`.m4a`) и Excel-таблицы (`.xlsx`)
теперь читаются и обычным `kf.py ingest` как любые другие форматы.

## История переименований и переносов

- `Структура хранилища/` → `Хранилище входных данных/` → `raw-data-repository/` (2026-07-12,
  затем 2026-07-16)
- `knowledge-factory/` → `Фабрика знаний (Цифровой мозг)/` → `Цифровой мозг (digital-brain)/` →
  `knowledge-factory/` (2026-07-11, 2026-07-12, затем 2026-07-16 — снова `knowledge-factory/`,
  на этот раз окончательно как часть общей англоязычной схемы имён папок верхнего уровня)
- `E:\knowledge-factory\` → `E:\Фабрика знаний (knowledge-factory)\` → встроена внутрь проекта
  как `Цифровой мозг (digital-brain)/data/` → `knowledge-factory/data/` (2026-07-12, 2026-07-16)
- `Файлы настройки проекта/` → `project-config/` (2026-07-16)
- **Весь проект перенесён с `C:\Users\Unknown\Documents\Projects\Digital brain\` на
  `E:\Digital brain\`** (2026-07-12), проверено побайтово (SHA256). Подробности —
  `project-config/РЕШЕНИЯ-И-СТАТУС.md` и
  `project-config/ПАМЯТЬ-СЕССИИ-ПЕРЕНОС.md`.
- **2026-07-16: три папки верхнего уровня переименованы на английский** (по решению
  пользователя, единая схема имён): `Хранилище входных данных/` → `raw-data-repository/`,
  `Файлы настройки проекта/` → `project-config/`, `Цифровой мозг (digital-brain)/` →
  `knowledge-factory/`. Содержимое (включая нетрекаемые `.env`, `data/`, `.venv`) перенесено
  как часть переименования папки — путь физически переехал, файлы не пересоздавались.
  Обновлены все ссылки на пути в живых файлах (`CLAUDE.md`, `AGENTS.md`, `.gitignore`,
  `kf/cli.py`, `kf/scope.py`, `README.md` внутри `knowledge-factory/`, `.env`) и
  MCP-регистрация `knowledge-factory` в `~/.claude.json`. Исторические логи и планы
  (`docs/superpowers/`, `Запросы и обращение к АИ.txt`) не переписывались — сохраняют
  старые имена как есть, это записи по состоянию на момент их создания.
- **`Запросы и обращение к АИ.txt` переехал с корня в `Разработкаи Обращение к АИ/`**,
  рядом появился новый файл `Запросы к АИ ... Выжимка по урокам.txt` (переезд сделан
  пользователем самостоятельно, не через ассистента).
